# KnowYourClaw Web Frontend Configuration

# API URL (for production, set to your deployed API URL)
VITE_API_URL=http://localhost:3000

# Admin Dashboard Path
# IMPORTANT: Set your own unique, hard-to-guess path
# Without this, the admin page won't be accessible at all
# Example: generate with: openssl rand -hex 16
VITE_ADMIN_PATH=your-secret-admin-path-here
