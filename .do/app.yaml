name: agentproof
region: nyc
services:
  - name: api
    github:
      repo: packtman/AgentDMV
      branch: main
      deploy_on_push: true
    dockerfile_path: Dockerfile
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 3000
    routes:
      - path: /
    envs:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: DATABASE_PATH
        value: /app/data/agentproof.db
    health_check:
      http_path: /health
      initial_delay_seconds: 15
      period_seconds: 30
